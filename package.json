{
  "name": "sysdevup-web",
  "version": "1.0.0",
  "private": true,
  "description": "Sitio estatico de SysDevUp. El CSS se edita en css/style.css y se compila a css/style.min.css, que es el que cargan las paginas.",
  "scripts": {
    "build:css": "cleancss -o css/style.min.css css/style.css"
  },
  "devDependencies": {
    "clean-css-cli": "^5.6.3"
  }
}
